---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  data-slot="card-footer"
  class={`flex items-center gap-4 text-sm text-muted-foreground ${className}`}
>
  <slot />
</div>
